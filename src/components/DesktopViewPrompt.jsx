import React from 'react';

/**
 * DesktopViewPrompt
 * 
 * Completely disabled: Returns null on all devices to ensure zero modal prompts
 * and zero body scroll locks across all viewports.
 */
export default function DesktopViewPrompt() {
  return null;
}

